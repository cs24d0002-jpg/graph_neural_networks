experiment_name: "cora_baselines"
dataset: "PubMed"  # <--- This must match the code exactly
model_type: "sage"  # <--- This must match the code exactly
hparams:
  hidden_channels: 64
  lr: 0.01
  epochs: 200
  dropout: 0.5