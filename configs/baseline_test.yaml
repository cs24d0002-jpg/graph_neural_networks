experiment_name: "cora_baselines"
dataset: "PPI"  # <--- This must match the code exactly
model_type: "gat_ppi"  # <--- This must match the code exactly
hparams:
  hidden_channels: 64
  lr: 0.01
  epochs: 200
  dropout: 0.5